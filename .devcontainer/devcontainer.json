{
	"name": "TeamTex",
	"dockerComposeFile": "docker-compose.yml",
	"service": "teamtex",
	"workspaceFolder": "/workspace",
	"updateContentCommand": "mkdir -p build && pdflatex -interaction=nonstopmode -output-directory=build main.tex && bibtex build/main || true && pdflatex -interaction=nonstopmode -output-directory=build main.tex && pdflatex -interaction=nonstopmode -output-directory=build main.tex",
	"customizations": {
		"vscode": {
			"extensions": [
				"James-Yu.latex-workshop",
				"GitHub.copilot",
				"ms-vsliveshare.vsliveshare"
				// Uncomment below for Grammar checking
				// "valentjn.vscode-ltex"
			],
			"files.exclude": {
				".devcontainer": true,
				".github": true,
				"docs": true,
				"LICENSE": true
			}
		}
	}
}
